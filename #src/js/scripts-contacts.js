$(function(){
   /* header menu */

   $('.menu').click(function(){
      $(this).toggleClass('menu--active');
      $('.header__inner').toggleClass('header__inner--active');
   })

   /* -- header menu end -- */

   /* scroll mouse */
   
});
